# Copy to ".env" for local dev, or set these in Netlify → Site settings → Environment.
# Leave BOTH blank to run in single-device Demo Mode (no backend, data saved in the browser).
# Fill BOTH in to enable real accounts + two-device sync via Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
